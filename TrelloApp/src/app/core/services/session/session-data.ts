export interface SessionData {
  accessToken: string,
  refreshToken: string,
  email: string,
  username: string,
  theme: string,
  avatarBackground: string
}

export interface BoardData {
  boardId: number
}
